# Server

A simple Elysia HTTP server running on port 3002.

## Development

```bash
bun run dev
```

The server will start on `http://localhost:3002`

## Endpoints

- `GET /` - Returns a simple greeting
- `GET /api/health` - Returns server health status
